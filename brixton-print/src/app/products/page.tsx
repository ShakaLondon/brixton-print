import Header from "@/components/header/header";
import { ReactNode } from "react";

interface ProductProps {
  children: ReactNode;
}

const MainLayout = ({ children }: ProductProps) => {
  return <div></div>;
};

export default MainLayout;
